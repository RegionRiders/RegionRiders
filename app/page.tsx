import { Navbar } from '@/components/Navbar/Navbar';

/**
 * Renders the homepage UI containing the top navigation bar.
 *
 * @returns The React element for the homepage which includes the `Navbar` component.
 */
export default function HomePage() {
  return (
    <>
      <Navbar/>
    </>
  );
}
